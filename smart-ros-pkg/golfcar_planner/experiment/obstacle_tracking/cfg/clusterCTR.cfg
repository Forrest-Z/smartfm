#!/usr/bin/env python
PACKAGE = "obstacle_tracking"
import roslib;roslib.load_manifest(PACKAGE)

from dynamic_reconfigure.parameter_generator import *

gen = ParameterGenerator()

gen.add("tolerate_value", double_t, 0, "cluster_tolerance", 0.8, 0, 20)
gen.add("min_cluster", int_t, 0, "min_cluster_size", 8, 0, 200)
gen.add("max_cluster", int_t, 0, "max_cluster_size", 80, 0, 10000)
gen.add("moving_avg_size", int_t, 0, "moving_avg_size", 2, 0, 10)

# Odometry Model Parameters
# odt = gen.enum([ gen.const("diff_const", str_t, "diff", "Use diff odom model"), gen.const("omni_const", str_t, "omni", "Use omni odom model")], "Odom Models")
#gen.add("odom_model_type", str_t, 0, "Which model to use, either diff or omni", "diff", edit_method = odt)

exit(gen.generate(PACKAGE, "obstacle_tracking", "clusterCTR"))
