1. Record pedestrian tracks:
	(provide localization before this)
	a. launch/pedestrian_detection.launch
	b. launch/collector.launch
	
2. Learning semantics using recorded pedestrian tracks:
./bin/ped_semantics -image-file ./launch/road_map.png -data-file ./data/ped_track.data -image-scale 0.5
